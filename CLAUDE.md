# Claude Code compatibility

@AGENTS.md

The repository rules are maintained in `AGENTS.md`. Claude-compatible skill copies are generated
in `.claude/skills/` by `./bin/wpx skills install`; edit the canonical `.agents/skills/` files only.
