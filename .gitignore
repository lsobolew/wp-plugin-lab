# Environment artefacts
.wplab/
env/docker-compose.gen.yml
env/docker-compose.override.yml
dist/
playwright-report/
test-results/

# Dependencies
node_modules/
vendor/

# Build output
plugins/*/build/
plugins/*/languages/*.mo
.phpunit.result.cache
.phpcs.cache

# The canonical agent skills in .agents/skills and the generated Claude compatibility copy in
# .claude/skills are committed on purpose, so updates remain reviewable for every agent.

# System
.DS_Store
*.log
